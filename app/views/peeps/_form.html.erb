<%= semantic_form_for(@peep) do |f| %>
  <%= f.inputs %>
  
  <% if @peep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peep.errors.count, "error") %> prohibited this peep from being saved:</h2>

      <ul>
      <% @peep.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
